/*8. Crie um programa que lê 6 valores inteiros e, em seguida, mostre na tela os valores lidos na 
ordem inversa.*/

#include <stdio.h>
#define tam 6

int main(){
	
	int i, v[tam];
	
	for (i=0; i< tam; i++){
		scanf("%d", &v[i]);
	}
	printf("\n");
	for (i=tam-1; i >= 0; i--){
		printf("%d ", v[i]);
	}
			
	return 0;
}