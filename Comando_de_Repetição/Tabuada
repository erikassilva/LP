//Dado um numero n (entre 1 e 10) exiba a sua tabuada

#include <stdio.h>

int main (void){
	int n, i;
	printf("Numero? ");
	scanf("%d", &n);
	for (i=1; i<=10; i++)
		printf("%d x %2d = %3d\n", n, i, n*i);
	return 0;
}
